# ── Orchestration ─────────────────────────────────────────────────────────────
langgraph>=0.2.0,<0.3.0
langchain>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0

# ── Exchange / Market Data ────────────────────────────────────────────────────
ccxt>=4.4.0,<5.0.0
yfinance>=0.2.50

# ── Polymarket ────────────────────────────────────────────────────────────────
py-clob-client>=0.18.0
websockets>=13.0

# ── Technical Analysis ────────────────────────────────────────────────────────
TA-Lib>=0.4.32              # system dep: brew install ta-lib / apt install libta-lib-dev
pandas-ta>=0.3.14           # pandas fallback; no system dep
pandas>=2.2.0,<3.0.0
numpy>=1.26.0               # no upper cap — pandas-ta 0.4.x requires >=2.2.6
scipy>=1.13.0

# ── Config / Validation ───────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-dotenv>=1.0.0

# ── API / Dashboard ───────────────────────────────────────────────────────────
fastapi>=0.115.0,<0.200.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0,<0.29.0

# ── Async / Networking ────────────────────────────────────────────────────────
aiohttp>=3.10.0,<4.0.0
asyncio-throttle>=1.0.2

# ── Persistence / Alerts ─────────────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0
asyncpg>=0.30.0             # optional: PostgreSQL; SQLite works without it
redis>=5.2.0,<6.0.0         # optional: pub/sub signal bus
python-telegram-bot>=21.0

# ── Observability ─────────────────────────────────────────────────────────────
structlog>=24.4.0
matplotlib>=3.9.0

# ── Dev ───────────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.8.0
mypy>=1.11.0
pre-commit>=3.8.0

# ── ML bridge (Step 5 — FreqAI-style) ─────────────────────────────────────────
# LightGBM is the primary backend; if unavailable the bridge falls back to
# scikit-learn's HistGradientBoostingClassifier (and finally a heuristic).
lightgbm>=4.3.0          # optional primary ML backend
scikit-learn>=1.4.0      # fallback ML backend (NaN-tolerant HistGBM)
joblib>=1.3.0            # model persistence
